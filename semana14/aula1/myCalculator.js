//  exercicio 1 
const numUm = Number(process.argv[3]);
const numDois = Number(process.argv[4]);

const add = numUm + numDois;

const sub = numUm - numDois;

const mult = numUm * numDois;

const div = numUm / numDois;

console.log("Resposta: ", add)